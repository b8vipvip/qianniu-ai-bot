<UserControl
    x:Class="Bot.Options.CtlRobotOptions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    MinWidth="650"
    MinHeight="660">
    <UserControl.Resources>
        <SolidColorBrush x:Key="PageBg" Color="#F7F9FC" />
        <SolidColorBrush x:Key="CardBg" Color="#FFFFFF" />
        <SolidColorBrush x:Key="Border" Color="#E6ECF2" />
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0 3 7 3" />
            <Setter Property="Padding" Value="11 5" />
            <Setter Property="MinHeight" Value="29" />
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource PageBg}" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="220" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource CardBg}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="10 10 10 8">
            <StackPanel>
                <TextBlock Text="API 接口管理" FontSize="19" FontWeight="Bold" Foreground="#1F2937" />
                <TextBlock Text="支持官方或中转站 OpenAI 兼容接口。优先级靠前的接口失败时，Bot 会自动切换到下一接口。" Foreground="#6B7280" Margin="0 6 0 0" TextWrapping="Wrap" />
                <TextBlock Text="新增和编辑使用独立确认窗口；列表中的 ApiKey 只显示脱敏值。" Foreground="#2563EB" Margin="0 4 0 0" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource CardBg}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8" Padding="10 7" Margin="10 0 10 8">
            <WrapPanel>
                <Button x:Name="btnAddEndpoint" Click="btnAddEndpointAdvanced_Click" ToolTip="打开表单新增接口">新增接口</Button>
                <Button x:Name="btnEditEndpoint" Click="btnEditEndpointAdvanced_Click" ToolTip="编辑当前选中的接口">编辑选中</Button>
                <Button x:Name="btnPasteRecognize" Click="btnPasteRecognize_Click" ToolTip="粘贴 JSON、残缺 JSON 或配置片段并自动识别">粘贴识别</Button>
                <Border Width="1" Height="28" Background="#D1D5DB" Margin="2 3 9 3" />
                <Button x:Name="btnDeleteEndpoint" Click="btnDeleteEndpoint_Click">删除</Button>
                <Button x:Name="btnMoveUp" Click="btnMoveUp_Click">上移</Button>
                <Button x:Name="btnMoveDown" Click="btnMoveDown_Click">下移</Button>
                <Border Width="1" Height="28" Background="#D1D5DB" Margin="2 3 9 3" />
                <Button x:Name="btnTestSelected" Click="btnTestSelectedAdvanced_Click" ToolTip="真实发送随机测试消息并验证模型回复">完整测试选中</Button>
                <Button x:Name="btnTestAll" Click="btnTestAllAdvanced_Click" ToolTip="依次测试全部接口的通信、鉴权和模型收发">完整测试全部</Button>
                <Border Width="1" Height="28" Background="#D1D5DB" Margin="2 3 9 3" />
                <Button x:Name="btnImportConfig" Click="btnImportConfig_Click">导入文件</Button>
                <Button x:Name="btnExportConfig" Click="btnExportConfig_Click">导出配置</Button>
            </WrapPanel>
        </Border>

        <Border Grid.Row="2" Background="{StaticResource CardBg}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8" Padding="10" Margin="10 0 10 8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="调度策略：" VerticalAlignment="Center" Foreground="#374151" Margin="0 0 8 0" />
                <ComboBox Grid.Column="1" x:Name="cmbStrategy" Height="28" Margin="0 0 12 0">
                    <ComboBoxItem Content="按优先级顺序调用" />
                    <ComboBoxItem Content="主备模式" />
                    <ComboBoxItem Content="低成本优先" />
                    <ComboBoxItem Content="加权轮询（预留）" />
                    <ComboBoxItem Content="场景路由（预留）" />
                </ComboBox>
                <TextBlock Grid.Column="2" Text="当前稳定策略：按优先级失败切换。其它策略保留配置入口。" VerticalAlignment="Center" Foreground="#6B7280" TextWrapping="Wrap" />
            </Grid>
        </Border>

        <Border Grid.Row="3" Background="{StaticResource CardBg}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8" Padding="8" Margin="10 0 10 8">
            <DataGrid x:Name="gridEndpoints"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeColumns="True"
                      CanUserSortColumns="True"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionChanged="gridEndpoints_SelectionChanged"
                      MouseDoubleClick="gridEndpoints_MouseDoubleClickAdvanced"
                      HeadersVisibility="Column"
                      RowHeight="30"
                      Height="202"
                      MinHeight="202"
                      FrozenColumnCount="3"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="启用" Binding="{Binding Enabled}" Width="50" />
                    <DataGridTextColumn Header="优先级" Binding="{Binding Priority}" Width="60" />
                    <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="120" />
                    <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="105" />
                    <DataGridTextColumn Header="BaseUrl" Binding="{Binding BaseUrl}" Width="240" />
                    <DataGridTextColumn Header="文本模型" Binding="{Binding TextModel}" Width="135" />
                    <DataGridTextColumn Header="视觉" Binding="{Binding VisionStatus}" Width="130" />
                    <DataGridTextColumn Header="ApiKey" Binding="{Binding ApiKeyMasked}" Width="125" />
                    <DataGridTextColumn Header="超时秒" Binding="{Binding TimeoutSeconds}" Width="65" />
                    <DataGridTextColumn Header="状态" Binding="{Binding LastStatus}" Width="280" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <GroupBox Grid.Row="4" Header="选中接口的 SystemPrompt（可留空，留空使用默认客服提示词）" Margin="10 0 10 8" Padding="8" Background="{StaticResource CardBg}" BorderBrush="{StaticResource Border}">
            <TextBox x:Name="txtSystemPrompt" AcceptsReturn="True" TextWrapping="Wrap" Height="90" VerticalScrollBarVisibility="Auto" TextChanged="txtSystemPrompt_TextChanged" />
        </GroupBox>

        <Border Grid.Row="5" Background="#EAF5FF" BorderBrush="#BBD7FF" BorderThickness="1" CornerRadius="8" Padding="8" Margin="10 0 10 10">
            <TextBox x:Name="txtApiTestResult"
                     Text="提示：双击列表可编辑；新增、识别、导入和测试后请点击窗口右下角【保存】。"
                     Foreground="#1C5DB6"
                     Background="Transparent"
                     BorderThickness="0"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     Height="78"
                     VerticalScrollBarVisibility="Auto" />
        </Border>
    </Grid>
</UserControl>