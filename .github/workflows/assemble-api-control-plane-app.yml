name: Assemble API control plane sources

on:
  push:
    branches:
      - feat/api-control-plane
    paths:
      - tools/api_control_plane_app_parts/**
      - tools/api_control_plane_test_parts/**
      - .github/workflows/assemble-api-control-plane-app.yml

permissions:
  contents: write

jobs:
  assemble:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feat/api-control-plane
          fetch-depth: 0

      - name: Assemble and validate sources
        shell: bash
        run: |
          mkdir -p services/api-control-plane/tests
          find tools/api_control_plane_app_parts -type f -name '*.part' -print0 \
            | sort -z \
            | xargs -0 cat > services/api-control-plane/app.py
          find tools/api_control_plane_test_parts -type f -name '*.part' -print0 \
            | sort -z \
            | xargs -0 cat > services/api-control-plane/tests/test_control_plane.py
          python -m py_compile services/api-control-plane/app.py services/api-control-plane/tests/test_control_plane.py
          test "$(grep -c '^from __future__ import annotations' services/api-control-plane/app.py)" -eq 1
          test "$(grep -c '^if __name__ == "__main__":' services/api-control-plane/app.py)" -eq 1

      - name: Commit assembled sources
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add services/api-control-plane/app.py services/api-control-plane/tests/test_control_plane.py
          if git diff --cached --quiet; then
            echo "sources are already assembled"
            exit 0
          fi
          git commit -m "feat: assemble API control plane sources"
          git push origin HEAD:feat/api-control-plane
